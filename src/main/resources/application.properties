spring.application.name=chatbot
# src/main/resources/application.properties
openai.api.key=${OPENAI_API_KEY}
# Use Render PORT environment variable, default 8080
server.port=${PORT:8080}

# Optional: Logging level
logging.level.root=INFO
# --- Database ---
spring.datasource.url=jdbc:postgresql://dpg-d5vum8buibrs73d3ala0-a:5432/chatbot_db_g8od
spring.datasource.username=chatbot_user
spring.datasource.password=deKvotbVS67n6PucRBWmpDcz913fBhYy

spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false


spring.main.allow-circular-references=true
